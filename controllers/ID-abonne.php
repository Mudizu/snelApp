<?php
    // $janvier='janvier' && $janvier=01;
    if($pay['mois']==='janvier') {
        $pay['mois'] = '01';
    }
    if($pay['mois']==='fevrier') {
        $pay['mois'] = '02';
    }
    if($pay['mois']==='mars') {
        $pay['mois'] = '03';
    }
    if($pay['mois']==='avril') {
        $pay['mois'] = '04';
    }
    if($pay['mois']==='mai') {
        $pay['mois'] = '05';
    }
    if($pay['mois']==='juin') {
        $pay['mois'] = '06';
    }
    if($pay['mois']==='juillet') {
        $pay['mois'] = '07';
    }
    if($pay['mois']==='aout') {
        $pay['mois'] = '08';
    }
    if($pay['mois']==='septembre') {
        $pay['mois'] = '09';
    }
    if($pay['mois']==='octobre') {
        $pay['mois'] = '10';
    }
    if($pay['mois']==='novembre') {
        $pay['mois'] = '11';
    }
    if($pay['mois']==='decembre') {
        $pay['mois'] = '12';
    }
    // $Mois=($consommation['Mois']);
    // $energie=$consommation['energie'];

    // $RecupNom = substr($OneAbonne['nom'], -3);

    // $RefFac='Ref.'.$Mois.$OneAbonne['IdAbonne'].'BT';

    // if($consommation['IdCompteur']===$OneAbonne['IdCompteur'])
    // {
    //     print_r($energie);
    // }